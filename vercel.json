{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install --registry=https://registry.npmjs.org/",
  "buildCommand": "npm run build",
  "rewrites": [
    {
      "source": "/",
      "destination": "/demo"
    }
  ]
}
